// This file groups the mock data so the API can be swapped to a database later.
const roles = require('./roles.json')
const volunteers = require('./volunteers.json')
const continuityNotes = require('./continuityNotes.json')

module.exports = {
  roles,
  volunteers,
  continuityNotes,
}
